<div>
    {{#each products}}
    <p>{{this.title}}</p>
    {{/each}}
    {{#if hasPrevPage}}
    <a href="/api/views/verproducts?page={{ prevPage }}"><button>Anterior</button></a>
    {{/if}}
    {{#if hasNextPage}}
    <a href="/api/views/verproducts?page={{ nextPage }}"><button>Siguiente</button></a>
    {{/if}}
</div>